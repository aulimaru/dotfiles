# Fixes
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = dbus-update-activation-environment --all
exec-once = sleep 1 && dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP # Some fix idk
exec-once = xwayland-satellite

# Cursor
# exec-once = hyprctl setcursor Bibata-Modern-Classic 24

# Bar, wallpaper
exec-once = swww-daemon; swww img ~/.config/hypr/wallpapers/$default_wallpaper
exec-once = waybar

# Input method
exec-once = fcitx5

# Core components (authentication, idle, notification daemon)
exec-once = systemctl --user start hyprpolkitagent
exec-once = hypridle
exec-once = swaync

# Audio
# exec-once = easyeffects --gapplication-service
exec-once = musnify-mpd
exec-once = mpDris2

# Clipboard history
exec-once = wl-paste --watch cliprust store
exec-once = wl-clip-persist -c both

# Start-ups
exec-once = [workspace 1 silent] librewolf
exec-once = [workspace 10 silent] keepassxc

# restart emacs to ensure envs are set correctly
exec-once = sleep 1 && systemctl --user restart emacs.service
